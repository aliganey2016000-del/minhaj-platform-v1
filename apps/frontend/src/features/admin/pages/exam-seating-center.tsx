import { useEffect, useMemo, useRef, useState, type FormEvent, type ReactNode } from 'react';
import { Download, FileSpreadsheet, MoreVertical, Pencil, Plus, RefreshCw, Search, Upload, X, Zap } from 'lucide-react';
import api from '../../../lib/axios';
import { BackButton } from '../../shared/components/back-button';

type Room = { _id: string; name: string; building?: string; capacity: number };
type Student = { _id: string; studentId: string; profile?: { firstName?: string; lastName?: string }; organization?: string; department?: string; className?: string; class?: string; shift?: string; shiftMode?: string; academicYear?: string };
type Allocation = { _id: string; academicYear: string; examType: 'mid' | 'final'; deskNumber?: string; room?: Room; student?: Student };
type RowPreview = { row: number; organization: string; department: string; className: string; shift: string; studentId: string; studentName: string; academicYear: string; examType: string; room: string; seat: string; status: 'valid' | 'error'; message?: string };
type Fields = { organization: string; department: string; className: string; shift: string; studentId: string; studentName: string; academicYear: string; examType: string; room: string; seat: string };
const columns = ['Organization','Department','Class','Shift','Student ID','Student Name','Academic Year','Exam Type','Room','Seat'];
const card = 'rounded-2xl border border-[var(--color-border-default)] bg-[var(--color-surface-primary)] shadow-card';
const input = 'w-full rounded-xl border border-[var(--color-border-default)] bg-[var(--color-surface-primary)] px-3.5 py-2.5 text-sm outline-none focus:border-primary-500 focus:ring-2 focus:ring-primary-500/20';
const readonly = `${input} bg-[var(--color-surface-secondary)] text-[var(--color-text-secondary)]`;
const nameOf = (s?: Student) => [s?.profile?.firstName,s?.profile?.lastName].filter(Boolean).join(' ') || 'Unknown Student';
const classOf = (s?: Student) => s?.className || s?.class || '';
const shiftOf = (s?: Student) => s?.shift || s?.shiftMode || '';
const typeLabel = (v?: string) => v === 'mid' ? 'Mid Exam' : v === 'final' ? 'Final' : v || '—';
const emptyFields: Fields = { organization:'',department:'',className:'',shift:'',studentId:'',studentName:'',academicYear:'',examType:'',room:'',seat:'' };

function Modal({ title, close, children }: { title:string; close:()=>void; children:ReactNode }) { return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-black/50 p-4" onClick={close}><div onClick={e=>e.stopPropagation()} className="max-h-[94vh] w-full max-w-6xl overflow-auto rounded-3xl bg-[var(--color-surface-primary)] p-6 shadow-2xl"><div className="mb-6 flex items-start justify-between gap-4"><div><h2 className="text-xl font-bold">{title}</h2><p className="mt-1 text-sm text-[var(--color-text-tertiary)]">Master seating plan — one room and seat for all subjects.</p></div><button type="button" onClick={close} className="rounded-lg p-1 hover:bg-[var(--color-surface-secondary)]"><X size={20}/></button></div>{children}</div></div>; }

function Actions({ onAdd,onImport,onExport,onAuto }: { onAdd:()=>void; onImport:()=>void; onExport:()=>void; onAuto:()=>void }) { const [open,setOpen]=useState(false); const ref=useRef<HTMLDivElement>(null); useEffect(()=>{if(!open)return;const close=(e:MouseEvent)=>{if(ref.current&&!ref.current.contains(e.target as Node))setOpen(false)};document.addEventListener('mousedown',close);return()=>document.removeEventListener('mousedown',close)},[open]); const run=(fn:()=>void)=>{setOpen(false);fn()}; return <div ref={ref} className="relative shrink-0"><button type="button" aria-label="Exam Seating Center actions" onClick={()=>setOpen(v=>!v)} className="inline-flex h-11 w-11 items-center justify-center rounded-xl border border-[var(--color-border-default)] hover:bg-[var(--color-surface-secondary)]"><MoreVertical size={21}/></button>{open&&<div className="absolute right-0 top-12 z-[120] w-60 rounded-2xl border border-[var(--color-border-default)] bg-[var(--color-surface-primary)] p-1.5 shadow-2xl"><button type="button" onClick={()=>run(onAuto)} className="flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm font-semibold text-primary-600 hover:bg-[var(--color-surface-secondary)]"><Zap size={17}/>Auto Generate Seating</button><button type="button" onClick={()=>run(onAdd)} className="flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm hover:bg-[var(--color-surface-secondary)]"><Plus size={17}/>Add Seating</button><button type="button" onClick={()=>run(onImport)} className="flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm hover:bg-[var(--color-surface-secondary)]"><Upload size={17}/>Import Excel</button><button type="button" onClick={()=>run(onExport)} className="flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm hover:bg-[var(--color-surface-secondary)]"><Download size={17}/>Export</button></div>}</div>; }

function Field({ label,value,onChange,editable=false,children }: { label:string; value?:string; onChange?:(v:string)=>void; editable?:boolean; children?:ReactNode }) { return <label className="block space-y-1.5"><span className="text-xs font-semibold text-[var(--color-text-tertiary)]">{label}</span>{children||<input className={editable?input:readonly} value={value||''} readOnly={!editable} onChange={e=>onChange?.(e.target.value)}/>}</label>; }
function FieldsGrid({ fields,setField,rooms,lockedStudent=false }: { fields:Fields; setField:(key:keyof Fields,value:string)=>void; rooms:Room[]; lockedStudent?:boolean }) { return <div className="grid gap-4 md:grid-cols-2"><Field label="Organization" value={fields.organization} onChange={v=>setField('organization',v)} editable={!lockedStudent}/><Field label="Department" value={fields.department} onChange={v=>setField('department',v)} editable={!lockedStudent}/><Field label="Class" value={fields.className} onChange={v=>setField('className',v)} editable={!lockedStudent}/><Field label="Shift" value={fields.shift} onChange={v=>setField('shift',v)} editable={!lockedStudent}/><Field label="Student ID" value={fields.studentId} onChange={v=>setField('studentId',v)} editable={!lockedStudent}/><Field label="Student Name" value={fields.studentName} onChange={v=>setField('studentName',v)} editable={!lockedStudent}/><Field label="Academic Year" value={fields.academicYear} onChange={v=>setField('academicYear',v)} editable/><Field label="Exam Type" editable><select className={input} value={fields.examType} onChange={e=>setField('examType',e.target.value)}><option value="">Select exam type...</option><option value="mid">Mid Exam</option><option value="final">Final</option></select></Field><Field label="Room" editable><select className={input} value={fields.room} onChange={e=>setField('room',e.target.value)} required><option value="">Select room...</option>{rooms.map(r=><option key={r._id} value={r.name}>{r.name} · {r.capacity} seats</option>)}</select></Field><Field label="Seat" value={fields.seat} onChange={v=>setField('seat',v)} editable/></div>; }

function AddModal({ rooms,close,onSaved }: { rooms:Room[]; close:()=>void; onSaved:()=>void }) { const [fields,setFields]=useState<Fields>(emptyFields);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const setField=(key:keyof Fields,value:string)=>setFields(v=>({...v,[key]:value}));const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await api.post('/exams/seating-plan',fields);onSaved();close()}catch(err:any){setError(err.response?.data?.message||'Failed to add seating')}finally{setBusy(false)}};return <Modal title="Add Seating" close={close}><form onSubmit={submit} className="space-y-5">{error&&<div className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}<FieldsGrid fields={fields} setField={setField} rooms={rooms}/><div className="flex justify-end gap-2"><button type="button" onClick={close} className="rounded-xl border px-4 py-2.5">Cancel</button><button disabled={busy} className="rounded-xl bg-primary-600 px-5 py-2.5 font-semibold text-white">{busy?'Saving...':'Add Seating'}</button></div></form></Modal>; }

function EditModal({ allocation,rooms,close,onSaved }: { allocation:Allocation; rooms:Room[]; close:()=>void; onSaved:()=>void }) { const s=allocation.student;const [fields,setFields]=useState<Fields>({organization:s?.organization||'',department:s?.department||'',className:classOf(s),shift:shiftOf(s),studentId:s?.studentId||'',studentName:nameOf(s),academicYear:allocation.academicYear||s?.academicYear||'',examType:allocation.examType,room:allocation.room?.name||'',seat:allocation.deskNumber||''});const [busy,setBusy]=useState(false);const [error,setError]=useState('');const setField=(key:keyof Fields,value:string)=>setFields(v=>({...v,[key]:value}));const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await api.patch(`/exams/seating-plan/${allocation._id}`,{room:fields.room,seat:fields.seat,academicYear:fields.academicYear,examType:fields.examType});onSaved();close()}catch(err:any){setError(err.response?.data?.message||'Failed to update seating')}finally{setBusy(false)}};return <Modal title="Edit Seating" close={close}><form onSubmit={submit} className="space-y-5">{error&&<div className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}<FieldsGrid fields={fields} setField={setField} rooms={rooms} lockedStudent/><div className="flex justify-end gap-2"><button type="button" onClick={close} className="rounded-xl border px-4 py-2.5">Cancel</button><button disabled={busy} className="rounded-xl bg-primary-600 px-5 py-2.5 font-semibold text-white">{busy?'Saving...':'Save Changes'}</button></div></form></Modal>; }

function ImportModal({ close,onImported }: { close:()=>void; onImported:()=>void }) { const [file,setFile]=useState<File|null>(null);const [rows,setRows]=useState<RowPreview[]>([]);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [previewed,setPreviewed]=useState(false);const preview=async()=>{if(!file)return;setBusy(true);setError('');try{const form=new FormData();form.append('file',file);const r=await api.post('/exams/seating-plan/import-preview',form);const data=r.data.data||[];setRows(data);setPreviewed(true);if(data.some((x:RowPreview)=>x.status==='error'))setError('Some rows contain errors. Correct the Excel file before importing.')}catch(err:any){setError(err.response?.data?.message||'Import preview failed')}finally{setBusy(false)}};const commit=async()=>{if(!file||!previewed||rows.some(r=>r.status==='error'))return;setBusy(true);try{const form=new FormData();form.append('file',file);await api.post('/exams/seating-plan/import',form);onImported();close()}catch(err:any){setError(err.response?.data?.message||'Import failed')}finally{setBusy(false)}};const template=async()=>{try{const r=await api.get('/exams/seating-template',{responseType:'blob'});const url=URL.createObjectURL(r.data);const a=document.createElement('a');a.href=url;a.download='exam-seating-master-template.xlsx';a.click();URL.revokeObjectURL(url)}catch(err:any){setError(err.response?.data?.message||'Template download failed')}};return <Modal title="Import Exam Seating" close={close}><div className="space-y-5"><div className="rounded-2xl border bg-[var(--color-surface-secondary)] p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="font-semibold">10 required Excel columns</p><p className="mt-1 text-xs text-[var(--color-text-tertiary)]">{columns.join(' · ')}</p></div><button type="button" onClick={template} className="inline-flex items-center gap-2 rounded-xl border px-3 py-2 text-sm"><FileSpreadsheet size={16}/>Download Template</button></div></div><input type="file" accept=".xlsx,.xls,.csv" onChange={e=>{setFile(e.target.files?.[0]||null);setPreviewed(false);setRows([]);setError('')}} className={input}/>{error&&<div className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}{rows.length>0&&<div className="overflow-x-auto rounded-2xl border"><table className="min-w-[1200px] w-full text-xs"><thead className="bg-[var(--color-surface-secondary)]"><tr>{['Row','Student ID','Student Name','Academic Year','Exam Type','Room','Seat','Status'].map(c=><th key={c} className="px-3 py-2 text-left">{c}</th>)}</tr></thead><tbody>{rows.slice(0,100).map(r=><tr key={r.row} className="border-t"><td className="px-3 py-2">{r.row}</td><td className="px-3 py-2">{r.studentId}</td><td className="px-3 py-2">{r.studentName}</td><td className="px-3 py-2">{r.academicYear}</td><td className="px-3 py-2">{r.examType}</td><td className="px-3 py-2">{r.room}</td><td className="px-3 py-2">{r.seat||'—'}</td><td className={`px-3 py-2 font-semibold ${r.status==='error'?'text-red-600':'text-emerald-600'}`}>{r.status==='error'?r.message:'Valid'}</td></tr>)}</tbody></table></div>}<div className="flex justify-end gap-2"><button type="button" onClick={close} className="rounded-xl border px-4 py-2.5">Cancel</button>{!previewed?<button type="button" disabled={!file||busy} onClick={preview} className="rounded-xl bg-primary-600 px-5 py-2.5 font-semibold text-white">{busy?'Checking...':'Preview Import'}</button>:<button type="button" disabled={busy||rows.some(r=>r.status==='error')} onClick={commit} className="rounded-xl bg-primary-600 px-5 py-2.5 font-semibold text-white">{busy?'Importing...':`Import ${rows.length} Rows`}</button>}</div></div></Modal>; }

function AutoGenerateModal({ close,onGenerated }: { close:()=>void; onGenerated:(msg:string)=>void }) { const [year,setYear]=useState('');const [type,setType]=useState('');const [perRoom,setPerRoom]=useState('12');const [seatMode,setSeatMode]=useState<'none'|'sequential'>('none');const [organization,setOrganization]=useState('');const [department,setDepartment]=useState('');const [className,setClassName]=useState('');const [shift,setShift]=useState('');const [overwrite,setOverwrite]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [result,setResult]=useState<any>(null);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');setResult(null);try{const r=await api.post('/exams/seating-plan/auto-generate',{academicYear:year,examType:type,maxPerRoom:Number(perRoom),seatMode,organization,department,className,shift,overwrite});setResult(r.data.data);onGenerated(r.data.message||'Seating generated successfully.')}catch(err:any){setError(err.response?.data?.message||'Automatic generation failed')}finally{setBusy(false)}};return <Modal title="Auto Generate Seating" close={close}><form onSubmit={submit} className="space-y-6"><div className="rounded-2xl border border-primary-200 bg-primary-50/50 p-4"><p className="font-semibold">Generate directly from existing student records</p><p className="mt-1 text-sm text-[var(--color-text-secondary)]">No Excel file is required. The system selects active students, splits them into balanced groups, and assigns each group to a room. The same Room + Seat remains valid for every subject.</p></div>{error&&<div className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}<div className="grid gap-4 md:grid-cols-2"><Field label="Academic Year" value={year} onChange={setYear} editable/><Field label="Exam Type" editable><select className={input} value={type} onChange={e=>setType(e.target.value)} required><option value="">Select...</option><option value="mid">Mid Exam</option><option value="final">Final</option></select></Field><Field label="Students per Room" editable><select className={input} value={perRoom} onChange={e=>setPerRoom(e.target.value)}><option value="10">10</option><option value="11">11</option><option value="12">12</option><option value="13">13</option><option value="14">14</option><option value="15">15</option></select></Field><Field label="Seat Assignment" editable><select className={input} value={seatMode} onChange={e=>setSeatMode(e.target.value as 'none'|'sequential')}><option value="none">Room only — seats optional</option><option value="sequential">Room + sequential seats</option></select></Field><Field label="Organization" value={organization} onChange={setOrganization} editable/><Field label="Department" value={department} onChange={setDepartment} editable/><Field label="Class" value={className} onChange={setClassName} editable/><Field label="Shift" value={shift} onChange={setShift} editable/></div><label className="flex items-start gap-3 rounded-xl border p-4"><input type="checkbox" checked={overwrite} onChange={e=>setOverwrite(e.target.checked)} className="mt-1"/><span><b>Regenerate existing plan</b><span className="block text-sm text-[var(--color-text-tertiary)]">Replace the current seating plan for this Academic Year + Exam Type.</span></span></label>{result&&<div className="rounded-2xl border bg-[var(--color-surface-secondary)] p-4"><div className="grid grid-cols-2 gap-3 text-sm md:grid-cols-4"><div><b>{result.students}</b><span className="block text-xs text-[var(--color-text-tertiary)]">Students</span></div><div><b>{result.rooms}</b><span className="block text-xs text-[var(--color-text-tertiary)]">Rooms</span></div><div><b>{result.studentsPerRoom}</b><span className="block text-xs text-[var(--color-text-tertiary)]">Target / room</span></div><div><b>{result.seatMode==='none'?'Optional':'Assigned'}</b><span className="block text-xs text-[var(--color-text-tertiary)]">Seats</span></div></div><div className="mt-4 space-y-2">{result.roomBreakdown?.map((r:any)=><div key={r.room} className="flex justify-between rounded-lg border bg-[var(--color-surface-primary)] px-3 py-2 text-sm"><span>{r.room}</span><span>{r.students} students / {r.capacity} capacity</span></div>)}</div></div>}<div className="flex justify-end gap-2"><button type="button" onClick={close} className="rounded-xl border px-4 py-2.5">Cancel</button><button disabled={busy||!year||!type} className="inline-flex items-center gap-2 rounded-xl bg-primary-600 px-5 py-2.5 font-semibold text-white disabled:opacity-50"><Zap size={16}/>{busy?'Generating...':'Generate Seating'}</button></div></form></Modal>; }

export function ExamSeatingCenter() { const [allocations,setAllocations]=useState<Allocation[]>([]);const [rooms,setRooms]=useState<Room[]>([]);const [loading,setLoading]=useState(true);const [query,setQuery]=useState('');const [academicYear,setAcademicYear]=useState('');const [examType,setExamType]=useState('');const [roomFilter,setRoomFilter]=useState('all');const [modal,setModal]=useState<'add'|'import'|'edit'|'auto'|null>(null);const [editing,setEditing]=useState<Allocation|null>(null);const [error,setError]=useState('');const [message,setMessage]=useState('');const load=async()=>{setLoading(true);setError('');try{const params=new URLSearchParams();if(academicYear.trim())params.set('academicYear',academicYear.trim());if(examType)params.set('examType',examType);const [a,r]=await Promise.all([api.get(`/exams/seating-plan${params.toString()?`?${params}`:''}`),api.get('/exams/seating-plan/rooms')]);setAllocations(a.data.data||[]);setRooms(r.data.data||[])}catch(err:any){setError(err.response?.data?.message||'Failed to load seating plan')}finally{setLoading(false)}};useEffect(()=>{load()},[academicYear,examType]);const filtered=useMemo(()=>allocations.filter(a=>{const text=[a.student?.organization,a.student?.department,classOf(a.student),shiftOf(a.student),a.student?.studentId,nameOf(a.student),a.academicYear,typeLabel(a.examType),a.room?.name,a.deskNumber].join(' ').toLowerCase();return(!query||text.includes(query.toLowerCase()))&&(roomFilter==='all'||a.room?._id===roomFilter)}),[allocations,query,roomFilter]);const exportCsv=()=>{const rows=[columns,...filtered.map(a=>[a.student?.organization||'',a.student?.department||'',classOf(a.student),shiftOf(a.student),a.student?.studentId||'',nameOf(a.student),a.academicYear,typeLabel(a.examType),a.room?.name||'',a.deskNumber||''])];const csv=rows.map(row=>row.map(v=>`"${String(v).replace(/"/g,'""')}"`).join(',')).join('\r\n');const url=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download=`exam-seating-${academicYear||'all'}-${examType||'all'}.csv`;a.click();URL.revokeObjectURL(url)};const done=(msg:string)=>{setMessage(msg);load();window.setTimeout(()=>setMessage(''),3500)};return <div className="p-6 pt-20 lg:p-10 lg:pt-10"><div className="mx-auto max-w-screen-2xl space-y-6"><BackButton fallback="/admin/exams"/><div className="flex items-start justify-between gap-4"><div><h1 className="text-3xl font-bold">Exam Seating Center</h1><p className="mt-1 text-sm text-[var(--color-text-tertiary)]">Master seating plan for all subjects. Each student keeps the same Room + Seat.</p></div><Actions onAuto={()=>setModal('auto')} onAdd={()=>setModal('add')} onImport={()=>setModal('import')} onExport={exportCsv}/></div>{error&&<div className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}{message&&<div className="rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{message}</div>}<div className={`${card} p-5`}><div className="grid gap-4 md:grid-cols-[1fr_220px_220px]"><label className="block space-y-1.5"><span className="text-xs font-semibold text-[var(--color-text-tertiary)]">Academic Year</span><input className={input} value={academicYear} onChange={e=>setAcademicYear(e.target.value)} placeholder="e.g. 2026/27"/></label><label className="block space-y-1.5"><span className="text-xs font-semibold text-[var(--color-text-tertiary)]">Exam Type</span><select className={input} value={examType} onChange={e=>setExamType(e.target.value)}><option value="">All Exam Types</option><option value="mid">Mid Exam</option><option value="final">Final</option></select></label><label className="block space-y-1.5"><span className="text-xs font-semibold text-[var(--color-text-tertiary)]">Room</span><select className={input} value={roomFilter} onChange={e=>setRoomFilter(e.target.value)}><option value="all">All Rooms</option>{rooms.map(r=><option key={r._id} value={r._id}>{r.name}</option>)}</select></label></div></div><div className={`${card} overflow-hidden`}><div className="flex flex-col gap-3 border-b p-5 lg:flex-row lg:items-center"><div className="relative flex-1"><Search size={17} className="absolute left-3 top-1/2 -translate-y-1/2"/><input className={`${input} pl-9`} value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search student, organization, department, class, room, seat..."/></div><button type="button" onClick={load} className="inline-flex items-center justify-center gap-2 rounded-xl border px-4 py-2.5 text-sm"><RefreshCw size={16}/>Refresh</button></div>{loading?<div className="p-20 text-center text-sm">Loading seating plan...</div>:filtered.length===0?<div className="p-20 text-center"><b>No seating assignments found</b><p className="mt-1 text-sm text-[var(--color-text-tertiary)]">Use ⋮ → Auto Generate, Add Seating, or Import Excel.</p></div>:<div className="overflow-x-auto"><table className="w-full min-w-[1500px] text-sm"><thead className="bg-[var(--color-surface-secondary)]"><tr>{[...columns,'Actions'].map(c=><th key={c} className="px-5 py-3 text-left text-xs font-semibold uppercase">{c}</th>)}</tr></thead><tbody>{filtered.map(a=><tr key={a._id} className="border-t hover:bg-[var(--color-surface-secondary)]/40"><td className="px-5 py-4">{a.student?.organization||'—'}</td><td className="px-5 py-4">{a.student?.department||'—'}</td><td className="px-5 py-4">{classOf(a.student)||'—'}</td><td className="px-5 py-4">{shiftOf(a.student)||'—'}</td><td className="px-5 py-4 font-semibold">{a.student?.studentId||'—'}</td><td className="px-5 py-4">{nameOf(a.student)}</td><td className="px-5 py-4">{a.academicYear}</td><td className="px-5 py-4">{typeLabel(a.examType)}</td><td className="px-5 py-4">{a.room?.name||'—'}</td><td className="px-5 py-4 font-semibold">{a.deskNumber||'—'}</td><td className="px-5 py-4"><button type="button" aria-label="Edit seating" onClick={()=>{setEditing(a);setModal('edit')}} className="rounded-lg p-2 hover:bg-[var(--color-surface-secondary)]"><Pencil size={17}/></button></td></tr>)}</tbody></table></div>}</div><div className="flex items-center justify-between text-xs text-[var(--color-text-tertiary)]"><span>{filtered.length} seating assignment{filtered.length===1?'':'s'}</span><span>Excel import replaces the existing plan for the same Academic Year + Exam Type.</span></div>{modal==='auto'&&<AutoGenerateModal close={()=>setModal(null)} onGenerated={msg=>{done(msg);setModal(null)}}/>}{modal==='add'&&<AddModal rooms={rooms} close={()=>setModal(null)} onSaved={()=>done('Seating added successfully.')}/>} {modal==='edit'&&editing&&<EditModal allocation={editing} rooms={rooms} close={()=>{setModal(null);setEditing(null)}} onSaved={()=>done('Seating updated successfully.')}/>} {modal==='import'&&<ImportModal close={()=>setModal(null)} onImported={()=>done('Seating plan imported successfully.')}/>}</div></div>; }
